# Bee

A small single-file browser game.

## Run

- Open `bee.html` in your browser.
- Or open the repo menu: `../gamemenu.html`.

## Notes

This folder contains a standalone HTML game with no build step.
